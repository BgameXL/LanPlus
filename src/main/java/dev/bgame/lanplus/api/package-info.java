/**
 * Shared contracts and value types for LAN+ (support layer, not a feature domain).
 *
 * Everything here is side-agnostic and dependency-free: no Minecraft/Forge
 * imports and no implementation. These are the abstractions every domain depends
 * on. The record shapes mirror.
 */
package dev.bgame.lanplus.api;
