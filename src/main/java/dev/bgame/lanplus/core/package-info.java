/**
 * Support layer: mod bootstrap and shared utilities (not a feature domain).
 *
 * Currently empty — the {@code @Mod} entry point lives at {@link dev.bgame.lanplus.Lanplus}.
 * Cross-cutting constants and helpers used by multiple domains belong here; feature logic does
 * not.
 */
package dev.bgame.lanplus.core;
