name: Publish (Modrinth + CurseForge)

on:
  release:
    types: [published]

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: zulu
          java-version: 17

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          validate-wrappers: true

      - name: Normalize version
        id: version
        run: |
          VERSION="${{ github.event.release.tag_name }}"
          VERSION="${VERSION#v}"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Apply release version to gradle.properties
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          sed -i "s/^mod_version=.*/mod_version=$VERSION/" gradle.properties

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Build
        run: ./gradlew build

      - name: Publish to Modrinth + CurseForge + GitHub Release
        uses: Kir-Antipov/mc-publish@v3.3.0
        with:
          files: build/libs/lanplus-${{ steps.version.outputs.version }}.jar

          name: "Lan+ ${{ steps.version.outputs.version }}"
          version: ${{ steps.version.outputs.version }}
          changelog: ${{ github.event.release.body }}

          version-type: ${{ github.event.release.prerelease && 'beta' || 'release' }}
          loaders: forge
          game-versions: |
            1.20.1
          java: 17

          modrinth-id: ${{ secrets.MODRINTH_PROJECT_ID }}
          modrinth-token: ${{ secrets.MODRINTH_TOKEN }}

          curseforge-id: ${{ secrets.CURSEFORGE_PROJECT_ID }}
          curseforge-token: ${{ secrets.CURSEFORGE_TOKEN }}

          github-token: ${{ secrets.GITHUB_TOKEN }}

          retry-attempts: 3
          retry-delay: 10000
          fail-mode: warn